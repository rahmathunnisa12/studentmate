<!--views/flashcards.ejs-->
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Flashcards</title>
  <!-- Google Font -->
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --bg: #f3f3fa; 
      --card-bg: #fff;
      --primary: #7c5ef7;
      --text: #333;
      --shadow: 0 4px 12px rgba(0, 0, 0, 0.05);
      --border-radius: 12px;
    }

    body {
      font-family: 'Inter', sans-serif;
      background-color: var(--bg);
      margin: 0;
      padding: 20px;
      display: flex;
      justify-content: center;
      min-height: 100vh;
    }

    .container {
      max-width: 800px;
      width: 100%;
    }

    h1 {
      text-align: center;
      color: var(--primary);
      margin-bottom: 30px;
    }

    .flashcard {
      background-color: var(--card-bg);
      border-radius: var(--border-radius);
      box-shadow: var(--shadow);
      padding: 20px;
      margin-bottom: 20px;
      transition: transform 0.2s ease;
    }

    .flashcard:hover {
      transform: scale(1.01);
    }

    .flashcard h3 {
      color: var(--primary);
      margin-bottom: 10px;
    }

    .flashcard p {
      color: var(--text);
    }

    .no-flashcards {
      text-align: center;
      color: #888;
      font-size: 18px;
      margin-top: 50px;
    }

    .nav-buttons {
      display: flex;
      justify-content: center;
      gap: 15px;
      margin-top: 40px;
    }

    .nav-buttons a {
      padding: 10px 20px;
      background-color: var(--primary);
      color: white;
      border-radius: var(--border-radius);
      text-decoration: none;
      transition: background-color 0.3s ease;
    }

    .nav-buttons a:hover {
      background-color: #6a4de6;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Your Flashcards</h1>

    <% var flashcards = flashcards || [] %>

<% if (flashcards.length === 0) { %>
  <div class="no-flashcards">
    <p>No flashcards found.</p>
    <p>Start by adding some notes from your dashboard.</p>
  </div>
<% } else { %>
  <% flashcards.forEach(card => { %>
    <div class="flashcard">
      <h3><%= card.title %></h3>
      <p><%= card.content.substring(0, 100) %>...</p>
    </div>
  <% }) %>
<% } %>

    <div class="nav-buttons">
      <a href="/dashboard">← Dashboard</a>
      <a href="/note/notes">📚 View Notes</a>
    </div>
  </div>
</body>
</html>